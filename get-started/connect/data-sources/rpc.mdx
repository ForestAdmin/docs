---
title: "RPC"
description: "Connect via RPC to Forest"
---

<Note>Only available for Node.js.</Note>

The RPC datasource allows you to connect to remote data sources via RPC (Remote Procedure Call), exposing their data and operations in your Forest back-office.

## Installation

```bash
npm install @forestadmin-experimental/datasource-rpc
```

## Basic usage

```javascript
import { createAgent } from '@forestadmin/agent';
import { createRpcDataSource } from '@forestadmin-experimental/datasource-rpc';

const agent = createAgent(options);

agent.addDataSource(
  createRpcDataSource({
    url: process.env.RPC_SERVER_URL
  })
);
```

## Source code

[github.com/ForestAdmin/forestadmin-experimental — datasource-rpc](https://github.com/ForestAdmin/forestadmin-experimental/tree/main/packages/datasource-rpc)
