---
title: "RPC"
description: "Connect via RPC to Forest Admin"
---

<Warning>
RPC datasource is only available for Node.js and is an experimental community package.
</Warning>

The RPC datasource allows you to connect to remote data sources via RPC (Remote Procedure Call), exposing their data and operations in your Forest Admin back-office.

## Installation

```bash
npm install @forestadmin-experimental/datasource-rpc
```

## Basic usage

```javascript
import { createAgent } from '@forestadmin/agent';
import { createRpcDataSource } from '@forestadmin-experimental/datasource-rpc';

const agent = createAgent(options);

agent.addDataSource(
  createRpcDataSource({
    url: process.env.RPC_SERVER_URL
  })
);
```

## Source code

This is a community datasource maintained on GitHub:

[https://github.com/ForestAdmin/forestadmin-experimental/tree/main/packages/datasource-rpc](https://github.com/ForestAdmin/forestadmin-experimental/tree/main/packages/datasource-rpc)
