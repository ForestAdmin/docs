---
title: forest init
description: Set up your Forest Admin development environment in your current project directory
---

# forest init

Set up your development environment in your current project directory. This command configures your local Forest Admin environment, linking your codebase to a project on Forest Admin.

## Usage

```bash
forest init [options]
```

## Options

| Option | Description |
|---|---|
| `-p, --projectId` | The ID of the project to initialize |

## What it does

`forest init` is an interactive wizard that:

1. Authenticates you if you are not already logged in
2. Selects the Forest Admin project to connect to
3. Configures your local agent endpoint (host and port)
4. Creates your **Development Environment** on Forest Admin
5. Optionally sets up your database connection
6. Creates or updates your `.env` file with the required environment variables

<Warning>
`forest init` is not meant to create a new project from scratch. If you do not have an existing Forest Admin project yet, create one from the UI first.
</Warning>

<Info>
Run `forest init` from your project's **root directory** — the same directory where your agent code lives.
</Info>

## Interactive prompts

### Authentication

If you are not already logged in, `forest init` will prompt for your credentials. You can also authenticate beforehand with [`forest login`](/reference/cli/login).

### Project selection

If your account is linked to multiple projects, you will be asked to select the one matching your current codebase:

```
? Select your project:
  ❯ My CRM Admin
    Internal Operations Tool
    Customer Support Panel
```

If your account has only one project, this step is skipped automatically.

### Endpoint configuration

Forest Admin needs to know where your local agent is running to set up the Development Environment:

```
? Enter your local agent host: (localhost)
? Enter your local agent port: (3310)
```

Press **Enter** to accept the defaults (`localhost:3310`) or provide custom values.

<Tip>
Your Development Environment is created as soon as you confirm the endpoint. 🎉
</Tip>

### Database configuration (optional)

`forest init` can help you set up your `DATABASE_URL` and related environment variables:

```
? Would you like to set up your database connection? (Y/n)
```

If you accept, you will be guided through entering your database credentials, which will be written to your `.env` file.

## Environment variables

After a successful `forest init`, the following variables are added to your `.env` file:

| Variable | Description |
|---|---|
| `FOREST_ENV_SECRET` | Secret key identifying your Development Environment |
| `DATABASE_URL` | Your database connection string (if configured) |

<Info>
The `FOREST_ENV_SECRET` uniquely identifies your Development Environment. Keep this value private and do not share it.
</Info>

## Example

```bash
$ cd my-forest-admin-project
$ forest init

✅ You are now logged in.
? Select your project: My CRM Admin
? Enter your local agent host: (localhost)
? Enter your local agent port: (3310)
✅ Development Environment created!
? Would you like to set up your database connection? Yes
? Enter your database URL: postgresql://user:password@localhost:5432/mydb
✅ Your .env file has been updated.
```

## Troubleshooting

**Not logged in**
Run [`forest login`](/reference/cli/login) first, or let `forest init` handle authentication interactively.

**Project not found**
Ensure your Forest Admin account has access to the project. Contact your project admin if needed.

**Endpoint unreachable**
Make sure your local agent is running before confirming the endpoint. Forest Admin will attempt to reach it during setup.